name: Deploy Docs

on:
  push:
    branches: [main]
    # The site is retired and now serves only a "docs have moved" notice, so it no
    # longer tracks the root markdown files it used to symlink.
    paths:
      - "mkdocs.yml"
      - "docs/**"
      - "overrides/**"
      - ".github/workflows/docs.yml"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v4
        with:
          version: "latest"

      - name: Set up Python
        run: uv python install 3.11

      - name: Install dependencies
        run: uv sync

      - name: Configure git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Deploy docs
        run: uv run mkdocs gh-deploy --force --clean
